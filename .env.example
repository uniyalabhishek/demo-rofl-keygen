# --- demo-rofl-keygen environment ---
# The container image used in compose.yaml (FQDN required; can include @sha256:<digest>)
# Example (Docker Hub): docker.io/<your-dockerhub-user>/demo-rofl-keygen:0.1.0
# Example (GHCR):       ghcr.io/<your-gh-username-or-org>/demo-rofl-keygen:0.1.0
IMAGE=docker.io/youruser/demo-rofl-keygen:0.1.0

# ROFL key parameters
KEY_ID=evm:base:sepolia

# EVM network (Base Sepolia by default)
BASE_RPC_URL=https://sepolia.base.org
BASE_CHAIN_ID=84532

# Local development ONLY (outside ROFL). If set, it's used when /run/rofl-appd.sock
# is unavailable. Must be a 0x-prefixed 32-byte hex secret key. DO NOT USE IN PROD.
# LOCAL_DEV_SK=0xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
# Explicitly allow LOCAL_DEV_SK fallback (ONLY when /run/rofl-appd.sock is missing).
# ALLOW_LOCAL_DEV=false
